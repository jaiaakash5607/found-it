{
  "name": "found-it-backend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "node ./dev-server-if-any.js || echo 'No dev server configured; use serverless functions.'",
    "build": "npx prisma generate --schema=./prisma/schema.prisma",
    "postinstall": "npx prisma generate --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "bcrypt": "^5.1.0",
    "cookie": "^0.6.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
